org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
kotlin.code.style=official
# Produkční build do wasm (`deployToSite`) se do 2 GB přestal vejít –
# padá na „Not enough memory to run compilation“. Vývojový `compileKotlinWasmJs`
# by si vystačil i s méně.
kotlin.daemon.jvmargs=-Xmx4g
